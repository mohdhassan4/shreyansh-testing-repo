name: OWASP Dependency Check Orchestration Security Scan

on:
  workflow_dispatch:
    inputs:
      harness-org-id:
        description: 'Harness Organization ID'
        required: true
        type: string
      harness-project-id:
        description: 'Harness Project ID'
        required: true
        type: string
      harness-domain:
        description: 'Harness domain (e.g., https://app.harness.io)'
        required: false
        type: string
        default: 'https://app.harness.io'

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Run OWASP default scan with default target
        id: sto-scan-default
        uses: ./.github/actions/harness-sto
        with:
          harness-token: ${{ secrets.HARNESS_TOKEN }}
          harness-org-id: ${{ inputs.harness-org-id }}
          harness-project-id: ${{ inputs.harness-project-id }}
          harness-domain: ${{ inputs.harness-domain }}

          scanner: owasp
          scan-mode: orchestration
          scan-config: default
          target-type: repository
          workspace: .
          tool-args: "--disableOssIndex"
          docker-image-tag: "dev"
          output-file: scan-output-default.env
          show-summary: 'true'

      - name: Run OWASP default scan with named target
        id: sto-scan-default-named
        if: always()
        uses: ./.github/actions/harness-sto
        with:
          harness-token: ${{ secrets.HARNESS_TOKEN }}
          harness-org-id: ${{ inputs.harness-org-id }}
          harness-project-id: ${{ inputs.harness-project-id }}
          harness-domain: ${{ inputs.harness-domain }}

          scanner: owasp
          scan-mode: orchestration
          scan-config: default
          target-type: repository
          workspace: .
          target-name: "shreyansh/owaspdefault"
          target-variant: "main"
          tool-args: "--disableOssIndex"
          docker-image-tag: "dev"
          output-file: scan-output-default-named.env
          show-summary: 'true'

     

      - name: Use scan output variables
        if: always()
        shell: bash
        run: |
          echo "===== STO Scan Output Variables (default target) ====="
          echo "Scan ID: ${{ steps.sto-scan-default.outputs.scan-id }}"
          echo "Status: ${{ steps.sto-scan-default.outputs.status }}"
          echo "Total Issues: ${{ steps.sto-scan-default.outputs.total-issues }}"
          echo "Critical: ${{ steps.sto-scan-default.outputs.critical }}"
          echo "High: ${{ steps.sto-scan-default.outputs.high }}"
          echo "Medium: ${{ steps.sto-scan-default.outputs.medium }}"
          echo "Low: ${{ steps.sto-scan-default.outputs.low }}"
          echo "Info: ${{ steps.sto-scan-default.outputs.info }}"
          echo "Output File Path: ${{ steps.sto-scan-default.outputs.output-file-path }}"

          echo "===== STO Scan Output Variables (default named) ====="
          echo "Scan ID: ${{ steps.sto-scan-default-named.outputs.scan-id }}"
          echo "Status: ${{ steps.sto-scan-default-named.outputs.status }}"
          echo "Total Issues: ${{ steps.sto-scan-default-named.outputs.total-issues }}"
          echo "Critical: ${{ steps.sto-scan-default-named.outputs.critical }}"
          echo "High: ${{ steps.sto-scan-default-named.outputs.high }}"
          echo "Medium: ${{ steps.sto-scan-default-named.outputs.medium }}"
          echo "Low: ${{ steps.sto-scan-default-named.outputs.low }}"
          echo "Info: ${{ steps.sto-scan-default-named.outputs.info }}"
          echo "Output File Path: ${{ steps.sto-scan-default-named.outputs.output-file-path }}"

          echo "===== STO Scan Output Variables (owasp default) ====="
          echo "Scan ID: ${{ steps.sto-scan-owasp-default.outputs.scan-id }}"
          echo "Status: ${{ steps.sto-scan-owasp-default.outputs.status }}"
          echo "Total Issues: ${{ steps.sto-scan-owasp-default.outputs.total-issues }}"
          echo "Critical: ${{ steps.sto-scan-owasp-default.outputs.critical }}"
          echo "High: ${{ steps.sto-scan-owasp-default.outputs.high }}"
          echo "Medium: ${{ steps.sto-scan-owasp-default.outputs.medium }}"
          echo "Low: ${{ steps.sto-scan-owasp-default.outputs.low }}"
          echo "Info: ${{ steps.sto-scan-owasp-default.outputs.info }}"
          echo "Output File Path: ${{ steps.sto-scan-owasp-default.outputs.output-file-path }}"

          echo "===== STO Scan Output Variables (owasp named) ====="
          echo "Scan ID: ${{ steps.sto-scan-owasp.outputs.scan-id }}"
          echo "Status: ${{ steps.sto-scan-owasp.outputs.status }}"
          echo "Total Issues: ${{ steps.sto-scan-owasp.outputs.total-issues }}"
          echo "Critical: ${{ steps.sto-scan-owasp.outputs.critical }}"
          echo "High: ${{ steps.sto-scan-owasp.outputs.high }}"
          echo "Medium: ${{ steps.sto-scan-owasp.outputs.medium }}"
          echo "Low: ${{ steps.sto-scan-owasp.outputs.low }}"
          echo "Info: ${{ steps.sto-scan-owasp.outputs.info }}"
          echo "Output File Path: ${{ steps.sto-scan-owasp.outputs.output-file-path }}"

        


      - name: Archive scan output (default)
        if: always() && steps.sto-scan-default.outputs.output-file-path != ''
        uses: actions/upload-artifact@v4
        with:
          name: sto-scan-results-default
          path: ${{ steps.sto-scan-default.outputs.output-file-path }}
          retention-days: 1

      - name: Archive scan output (default named)
        if: always() && steps.sto-scan-default-named.outputs.output-file-path != ''
        uses: actions/upload-artifact@v4
        with:
          name: sto-scan-results-default-named
          path: ${{ steps.sto-scan-default-named.outputs.output-file-path }}
          retention-days: 1

      - name: Archive scan output (owasp default)
        if: always() && steps.sto-scan-owasp-default.outputs.output-file-path != ''
        uses: actions/upload-artifact@v4
        with:
          name: sto-scan-results-owasp-default
          path: ${{ steps.sto-scan-owasp-default.outputs.output-file-path }}
          retention-days: 1

      - name: Archive scan output (owasp named)
        if: always() && steps.sto-scan-owasp.outputs.output-file-path != ''
        uses: actions/upload-artifact@v4
        with:
          name: sto-scan-results-owasp
          path: ${{ steps.sto-scan-owasp.outputs.output-file-path }}
          retention-days: 1

     